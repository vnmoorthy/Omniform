// Tailwind CSS v4 uses CSS-first configuration.
// Theme tokens (colors, animations, keyframes) live in app/globals.css
// inside the `@theme` block. This file is intentionally a no-op and is not
// loaded by Tailwind v4 unless referenced via an `@config` directive.
export {};
